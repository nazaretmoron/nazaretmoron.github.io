<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Nazaret Morón - Analista de Datos. De la experiencia del cliente a los insights de datos.">
    <title>Nazaret Morón | Analista de Datos</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Playfair+Display:wght@400;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome para iconos -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Tag Manager - REEMPLAZA GTM-XXXXXX con tu ID real -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-XXXXXX');</script>
    <!-- End Google Tag Manager -->
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --burgundy: #68192E;
            --burgundy-dark: #3D0E1A;
            --pink-neon: #FF1493;
            --pink-intense: #C9184A;
            --rose-gold: #E0AC9C;
            --champagne: #F7E7CE;
            --white: #FAFAFA;
            --text-dark: #2B2B2B;
        }

        body {
            font-family: 'Inter', sans-serif;
            color: var(--text-dark);
            background: var(--champagne);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--burgundy) 0%, var(--burgundy-dark) 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, var(--pink-neon) 0%, transparent 70%);
            opacity: 0.1;
            animation: pulse 8s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.1; }
            50% { transform: scale(1.1); opacity: 0.15; }
        }

        .hero-content {
            text-align: center;
            z-index: 2;
            padding: 2rem;
        }

        .hero-image {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid var(--rose-gold);
            box-shadow: 0 0 30px rgba(255, 20, 147, 0.4),
                        0 0 60px rgba(255, 20, 147, 0.2);
            margin-bottom: 2rem;
            animation: glow 3s ease-in-out infinite;
        }

        @keyframes glow {
            0%, 100% { box-shadow: 0 0 30px rgba(255, 20, 147, 0.4), 0 0 60px rgba(255, 20, 147, 0.2); }
            50% { box-shadow: 0 0 40px rgba(255, 20, 147, 0.6), 0 0 80px rgba(255, 20, 147, 0.3); }
        }

        .hero-name {
            font-family: 'Great Vibes', cursive;
            font-size: 4rem;
            color: var(--white);
            margin-bottom: 0.5rem;
            background: linear-gradient(90deg, var(--pink-neon), var(--rose-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 20px rgba(255, 20, 147, 0.3);
        }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--champagne);
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 1.5rem;
        }

        .hero-tagline {
            font-size: 1.2rem;
            color: var(--white);
            max-width: 600px;
            margin: 0 auto 2rem;
            font-weight: 300;
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2.5rem;
            background: linear-gradient(135deg, var(--pink-neon), var(--pink-intense));
            color: var(--white);
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 20, 147, 0.4);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 25px rgba(255, 20, 147, 0.6);
        }

        /* Section Styling */
        section {
            padding: 5rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            color: var(--burgundy);
            margin-bottom: 1rem;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--pink-neon), var(--rose-gold));
        }

        /* About Section */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            margin-top: 3rem;
        }

        .about-text {
            font-size: 1.1rem;
            line-height: 1.8;
        }

        .about-text p {
            margin-bottom: 1.5rem;
        }

        .highlight {
            color: var(--pink-intense);
            font-weight: 600;
        }

        /* Skills Section */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .skill-category {
            background: var(--white);
            padding: 2rem;
            border-radius: 15px;
            border-left: 4px solid var(--pink-neon);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .skill-category:hover {
            transform: translateY(-5px);
        }

        .skill-category h3 {
            font-family: 'Playfair Display', serif;
            color: var(--burgundy);
            margin-bottom: 1rem;
            font-size: 1.3rem;
        }

        .skill-category ul {
            list-style: none;
        }

        .skill-category li {
            padding: 0.5rem 0;
            color: var(--text-dark);
        }

        .skill-category li::before {
            content: '▹';
            color: var(--pink-neon);
            font-weight: bold;
            margin-right: 0.5rem;
        }

        /* Projects Section */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .project-card {
            background: var(--white);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 25px rgba(255, 20, 147, 0.3);
        }

        .project-header {
            background: linear-gradient(135deg, var(--burgundy), var(--burgundy-dark));
            padding: 1.5rem;
            color: var(--white);
        }

        .project-header h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .project-body {
            padding: 1.5rem;
        }

        .project-body p {
            margin-bottom: 1rem;
            color: var(--text-dark);
        }

        .project-tools {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin: 1rem 0;
        }

        .tool-tag {
            background: var(--champagne);
            color: var(--burgundy);
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.85rem;
            border: 1px solid var(--rose-gold);
        }

        .project-link {
            display: inline-block;
            color: var(--pink-neon);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        .project-link:hover {
            color: var(--pink-intense);
        }

        /* Contact Section */
        .contact-content {
            text-align: center;
            max-width: 600px;
            margin: 3rem auto;
        }

        .contact-links {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin: 2rem 0;
        }

        .contact-link {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
            color: var(--burgundy);
            transition: all 0.3s ease;
        }

        .contact-link:hover {
            transform: translateY(-5px);
            color: var(--pink-neon);
        }

        .contact-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--pink-neon), var(--pink-intense));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 1.5rem;
        }

        /* Footer */
        footer {
            background: var(--burgundy);
            color: var(--champagne);
            text-align: center;
            padding: 2rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-name {
                font-size: 3rem;
            }

            .about-content {
                grid-template-columns: 1fr;
            }

            .section-title {
                font-size: 2rem;
            }

            .contact-links {
                flex-direction: column;
            }
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-XXXXXX"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <img src="https://via.placeholder.com/200" alt="Nazaret Morón" class="hero-image">
            <h1 class="hero-name">Nazaret Morón</h1>
            <p class="hero-title">Analista de Datos</p>
            <p class="hero-tagline">Convierto experiencias de cliente en insights accionables</p>
            <a href="#proyectos" class="cta-button">Ver mis proyectos</a>
        </div>
    </section>

    <!-- About Section -->
    <section id="sobre-mi">
        <h2 class="section-title">Sobre mí</h2>
        <div class="about-content">
            <div class="about-text">
                <p>Mi trayectoria profesional combina dos mundos que, lejos de ser opuestos, se complementan perfectamente: la <span class="highlight">experiencia directa con clientes en el sector turismo</span> y el <span class="highlight">rigor técnico de la analítica digital</span>.</p>
                <p>Durante años trabajé como agente de reservas, donde aprendí a entender las necesidades del cliente, anticipar comportamientos y optimizar cada punto del customer journey. Ahora, aplico esa comprensión profunda del usuario combinada con herramientas de análisis de datos para generar insights que realmente impactan en el negocio.</p>
            </div>
            <div class="about-text">
                <p>Recientemente completé mi formación en <span class="highlight">Analítica Digital y CRO</span>, donde adquirí competencias técnicas en SQL, Google Analytics 4, GTM y herramientas de visualización de datos.</p>
                <p>No soy solo una analista de datos más: soy alguien que <span class="highlight">entiende qué hay detrás de cada métrica</span> porque he vivido la experiencia del cliente en primera persona.</p>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="habilidades">
        <h2 class="section-title">Habilidades y Herramientas</h2>
        <div class="skills-grid">
            <div class="skill-category">
                <h3><i class="fas fa-chart-line"></i> Análisis y Datos</h3>
                <ul>
                    <li>Excel y Google Sheets avanzado</li>
                    <li>SQL (consultas, joins, agregaciones)</li>
                    <li>Google Analytics 4</li>
                    <li>Adobe Analytics (nivel inicial)</li>
                    <li>Análisis de métricas digitales</li>
                </ul>
            </div>
            <div class="skill-category">
                <h3><i class="fas fa-globe"></i> Web Analytics y CRO</h3>
                <ul>
                    <li>Google Tag Manager</li>
                    <li>Análisis de embudos de conversión</li>
                    <li>Comportamiento de usuario</li>
                    <li>Optimización de conversión</li>
                    <li>Herramientas cualitativas</li>
                </ul>
            </div>
            <div class="skill-category">
                <h3><i class="fas fa-users"></i> Soft Skills</h3>
                <ul>
                    <li>Comprensión del customer journey</li>
                    <li>Comunicación de insights</li>
                    <li>Enfoque en negocio</li>
                    <li>Resolución de problemas</li>
                    <li>Trabajo en equipo</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Projects Section -->
    <section id="proyectos">
        <h2 class="section-title">Proyectos</h2>
        <div class="projects-grid">
            <div class="project-card">
                <div class="project-header">
                    <h3>Proyecto 1</h3>
                    <p>Análisis de datos</p>
                </div>
                <div class="project-body">
                    <p>Descripción del proyecto y principales insights obtenidos.</p>
                    <div class="project-tools">
                        <span class="tool-tag">Excel</span>
                        <span class="tool-tag">SQL</span>
                        <span class="tool-tag">GA4</span>
                    </div>
                    <a href="#" class="project-link">Ver caso completo →</a>
                </div>
            </div>

            <div class="project-card">
                <div class="project-header">
                    <h3>Proyecto 2</h3>
                    <p>Optimización de conversión</p>
                </div>
                <div class="project-body">
                    <p>Descripción del proyecto y principales insights obtenidos.</p>
                    <div class="project-tools">
                        <span class="tool-tag">GTM</span>
                        <span class="tool-tag">GA4</span>
                        <span class="tool-tag">Excel</span>
                    </div>
                    <a href="#" class="project-link">Ver caso completo →</a>
                </div>
            </div>

            <div class="project-card">
                <div class="project-header">
                    <h3>Proyecto 3</h3>
                    <p>Customer Analytics</p>
                </div>
                <div class="project-body">
                    <p>Descripción del proyecto y principales insights obtenidos.</p>
                    <div class="project-tools">
                        <span class="tool-tag">SQL</span>
                        <span class="tool-tag">Google Sheets</span>
                        <span class="tool-tag">GA4</span>
                    </div>
                    <a href="#" class="project-link">Ver caso completo →</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Formation Section -->
    <section id="formacion">
        <h2 class="section-title">Formación</h2>
        <div class="about-text" style="margin-top: 2rem;">
            <p><strong class="highlight">Bootcamp en Analítica Digital y CRO</strong><br>
            Moa Institute | 2024</p>
            
            <p><strong class="highlight">Experiencia en Sector Turismo</strong><br>
            Agente de Reservas Senior | Varios años</p>
            
            <p><em>Certificaciones y formación complementaria en análisis de datos, Google Analytics 4, y herramientas de optimización digital.</em></p>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contacto">
        <h2 class="section-title">¿Hablamos?</h2>
        <div class="contact-content">
            <p style="font-size: 1.2rem; margin-bottom: 2rem;">
                ¿Buscas una analista que entienda tanto al cliente como los datos? Me encantaría conocer cómo puedo aportar a tu equipo.
            </p>
            <div class="contact-links">
                <a href="https://www.linkedin.com/in/nazaretmoron" target="_blank" class="contact-link">
                    <div class="contact-icon">
                        <i class="fab fa-linkedin-in"></i>
                    </div>
                    <span>LinkedIn</span>
                </a>
                <a href="mailto:data.nazaret@gmail.com" class="contact-link">
                    <div class="contact-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <span>Email</span>
                </a>
                <a href="#" class="contact-link" id="cv-download">
                    <div class="contact-icon">
                        <i class="fas fa-file-download"></i>
                    </div>
                    <span>Descargar CV</span>
                </a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 Nazaret Morón | Analista de Datos</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Diseñado con pasión por los datos y la experiencia del usuario</p>
    </footer>

    <script>
        // Smooth scroll para los enlaces internos
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Tracking de eventos para GTM (una vez configures tu GTM)
        document.getElementById('cv-download').addEventListener('click', function(e) {
            e.preventDefault();
            // Aquí irá tu lógica de descarga del CV
            window.dataLayer = window.dataLayer || [];
            window.dataLayer.push({
                'event': 'cv_download',
                'eventCategory': 'engagement',
                'eventAction': 'click',
                'eventLabel': 'CV Download'
            });
            alert('Funcionalidad de descarga de CV - Por implementar');
        });

        // Tracking de clics en proyectos
        document.querySelectorAll('.project-link').forEach(link => {
            link.addEventListener('click', function(e) {
                const projectName = this.closest('.project-card').querySelector('h3').textContent;
                window.dataLayer = window.dataLayer || [];
                window.dataLayer.push({
                    'event': 'project_view',
                    'eventCategory': 'projects',
                    'eventAction': 'click',
                    'eventLabel': projectName
                });
            });
        });

        // Intersection Observer para animaciones al scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Aplicar animaciones a las secciones
        document.querySelectorAll('section').forEach(section => {
            section.style.opacity = '0';
            section.style.transform = 'translateY(20px)';
            section.style.transition = 'all 0.6s ease-out';
            observer.observe(section);
        });
    </script>
</body>
</html>
